-- GENERATED by `python -m fhirhouse_views.dbt_gen` from views/definitions/base/substance_protein.ViewDefinition.json
-- Do not hand-edit; edit the ViewDefinition and regenerate (FH-0005: the
-- ViewDefinition is the only source artifact).

WITH sof_src AS (
SELECT CAST(body_json AS JSON) AS resource, fhir_id AS resource_key
FROM {{ delta_table('silver', 'substanceprotein') }}
WHERE NOT coalesce(deleted, FALSE)
)
SELECT
  sof_text(sof_wrap(to_json(sof_src.resource_key))) AS "substance_protein_id",
  sof_text(sof_get(sof_wrap(sof_src.resource), '$."implicitRules"')) AS "implicit_rules",
  sof_text(sof_get(sof_wrap(sof_src.resource), '$."language"')) AS "language",
  sof_text(sof_get(sof_get(sof_wrap(sof_src.resource), '$."sequenceType"'), '$."text"')) AS "sequence_type_text",
  sof_text(sof_get(sof_get(sof_get(sof_wrap(sof_src.resource), '$."sequenceType"'), '$."coding"')[1:1], '$."code"')) AS "sequence_type_code",
  sof_text(sof_get(sof_get(sof_get(sof_wrap(sof_src.resource), '$."sequenceType"'), '$."coding"')[1:1], '$."system"')) AS "sequence_type_system",
  CAST(sof_text(sof_get(sof_wrap(sof_src.resource), '$."numberOfSubunits"')) AS INTEGER) AS "number_of_subunits",
  CAST(to_json(sof_get(sof_wrap(sof_src.resource), '$."disulfideLinkage"')) AS VARCHAR) AS "disulfide_linkage"
FROM sof_src
