-- GENERATED by `python -m fhirhouse_views.dbt_gen` from views/definitions/base/search_parameter.ViewDefinition.json
-- Do not hand-edit; edit the ViewDefinition and regenerate (FH-0005: the
-- ViewDefinition is the only source artifact).

WITH sof_src AS (
SELECT CAST(body_json AS JSON) AS resource, fhir_id AS resource_key
FROM delta_scan('${DELTA_BASE}/silver/searchparameter')
WHERE NOT coalesce(deleted, FALSE)
)
SELECT
  sof_text(sof_wrap(to_json(sof_src.resource_key))) AS "search_parameter_id",
  sof_text(sof_get(sof_wrap(sof_src.resource), '$."implicitRules"')) AS "implicit_rules",
  sof_text(sof_get(sof_wrap(sof_src.resource), '$."language"')) AS "language",
  sof_text(sof_get(sof_wrap(sof_src.resource), '$."url"')) AS "url",
  sof_text(sof_get(sof_wrap(sof_src.resource), '$."version"')) AS "version",
  sof_text(sof_get(sof_wrap(sof_src.resource), '$."name"')) AS "name",
  sof_text(sof_get(sof_wrap(sof_src.resource), '$."derivedFrom"')) AS "derived_from",
  sof_text(sof_get(sof_wrap(sof_src.resource), '$."status"')) AS "status",
  CAST(sof_text(sof_get(sof_wrap(sof_src.resource), '$."experimental"')) AS BOOLEAN) AS "experimental",
  sof_text(sof_get(sof_wrap(sof_src.resource), '$."date"')) AS "date",
  sof_text(sof_get(sof_wrap(sof_src.resource), '$."publisher"')) AS "publisher",
  sof_text(sof_get(sof_wrap(sof_src.resource), '$."description"')) AS "description",
  sof_text(sof_get(sof_wrap(sof_src.resource), '$."purpose"')) AS "purpose",
  sof_text(sof_get(sof_wrap(sof_src.resource), '$."code"')) AS "code",
  CAST(to_json(sof_get(sof_wrap(sof_src.resource), '$."base"')) AS VARCHAR) AS "base",
  sof_text(sof_get(sof_wrap(sof_src.resource), '$."type"')) AS "type",
  sof_text(sof_get(sof_wrap(sof_src.resource), '$."expression"')) AS "expression",
  sof_text(sof_get(sof_wrap(sof_src.resource), '$."xpath"')) AS "xpath",
  sof_text(sof_get(sof_wrap(sof_src.resource), '$."xpathUsage"')) AS "xpath_usage",
  CAST(to_json(sof_get(sof_wrap(sof_src.resource), '$."target"')) AS VARCHAR) AS "target",
  CAST(sof_text(sof_get(sof_wrap(sof_src.resource), '$."multipleOr"')) AS BOOLEAN) AS "multiple_or",
  CAST(sof_text(sof_get(sof_wrap(sof_src.resource), '$."multipleAnd"')) AS BOOLEAN) AS "multiple_and",
  CAST(to_json(sof_get(sof_wrap(sof_src.resource), '$."comparator"')) AS VARCHAR) AS "comparator",
  CAST(to_json(sof_get(sof_wrap(sof_src.resource), '$."modifier"')) AS VARCHAR) AS "modifier",
  CAST(to_json(sof_get(sof_wrap(sof_src.resource), '$."chain"')) AS VARCHAR) AS "chain"
FROM sof_src;
