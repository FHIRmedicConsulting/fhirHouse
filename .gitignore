# --- Secrets: never commit real credential values (*.env.op holds only op:// refs) ---
.env
*.env

# --- Dependencies / build output ---
node_modules/
dist/
*.tsbuildinfo

# --- Test/build scratch ---
*.log
.DS_Store
vitest.config.ts.timestamp-*.mjs
*.tmp

# --- Local Delta stores (operational + any licensed-derived terminology) ---
delta/
.delta*/
delta-prov/
.ronin-export/

# --- Operator-licensed terminology release files — NEVER commit/redistribute ---
# (SNOMED CT, LOINC, RxNorm, etc.; loaded under the operator's own license)
terminologies/
*.RRF
sct2_*.txt
der2_*.txt

# --- Python sidecar venv ---
sidecar/.venv/
**/__pycache__/

coverage/

# --- fhirHouse additions (keep as one block; upstream owns the sections above) ---
# DuckDB database files (built views over PHI-bearing Delta stores; *.duckdb.sql
# compiled-SQL text files in views/compiled/ remain tracked)
*.duckdb
*.duckdb.wal*
# local demo/bench Delta stores if placed in-repo (primary stores live outside)
fhirhouse-demo/
bench-delta*/
# Python env + packaging litter
.venv/
*.egg-info/
# Splink model artifacts (trained on operator data — never redistribute)
splink_model*.json
