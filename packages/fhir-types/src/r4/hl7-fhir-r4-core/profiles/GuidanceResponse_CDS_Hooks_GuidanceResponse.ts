// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";
import type { GuidanceResponse } from "../../hl7-fhir-r4-core/GuidanceResponse";
import type { Identifier } from "../../hl7-fhir-r4-core/Identifier";

import { cdsHooksEndpointProfile } from "./Extension_cdsHooksEndpoint";

import {
    buildResource,
    ensureProfile,
    isExtension,
    getExtensionValue,
    pushExtension,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type CDS_Hooks_GuidanceResponseProfileRaw = {
    requestIdentifier: Identifier;
    identifier: Identifier[];
    status: ("success" | "data-requested" | "data-required" | "in-progress" | "failure" | "entered-in-error");
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/cdshooksguidanceresponse (pkg: hl7.fhir.r4.core#4.0.1)
export class CDS_Hooks_GuidanceResponseProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/cdshooksguidanceresponse";

    private resource: GuidanceResponse;

    constructor (resource: GuidanceResponse) {
        this.resource = resource;
    }

    static from (resource: GuidanceResponse) : CDS_Hooks_GuidanceResponseProfile {
        if (!resource.meta?.profile?.includes(CDS_Hooks_GuidanceResponseProfile.canonicalUrl)) {
            throw new Error(`CDS_Hooks_GuidanceResponseProfile: meta.profile must include ${CDS_Hooks_GuidanceResponseProfile.canonicalUrl}`)
        }
        const profile = new CDS_Hooks_GuidanceResponseProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: GuidanceResponse) : CDS_Hooks_GuidanceResponseProfile {
        ensureProfile(resource, CDS_Hooks_GuidanceResponseProfile.canonicalUrl);
        return new CDS_Hooks_GuidanceResponseProfile(resource);
    }

    static createResource (args: CDS_Hooks_GuidanceResponseProfileRaw) : GuidanceResponse {
        const resource = buildResource<GuidanceResponse>( {
            resourceType: "GuidanceResponse",
            requestIdentifier: args.requestIdentifier,
            identifier: args.identifier,
            status: args.status,
            meta: { profile: [CDS_Hooks_GuidanceResponseProfile.canonicalUrl] },
        })
        return resource;
    }

    static create (args: CDS_Hooks_GuidanceResponseProfileRaw) : CDS_Hooks_GuidanceResponseProfile {
        return CDS_Hooks_GuidanceResponseProfile.apply(CDS_Hooks_GuidanceResponseProfile.createResource(args));
    }

    toResource () : GuidanceResponse {
        return this.resource;
    }

    // Field accessors
    getRequestIdentifier () : Identifier | undefined {
        return this.resource.requestIdentifier as Identifier | undefined;
    }

    setRequestIdentifier (value: Identifier) : this {
        Object.assign(this.resource, { requestIdentifier: value });
        return this;
    }

    getIdentifier () : Identifier[] | undefined {
        return this.resource.identifier as Identifier[] | undefined;
    }

    setIdentifier (value: Identifier[]) : this {
        Object.assign(this.resource, { identifier: value });
        return this;
    }

    getStatus () : ("success" | "data-requested" | "data-required" | "in-progress" | "failure" | "entered-in-error") | undefined {
        return this.resource.status as ("success" | "data-requested" | "data-required" | "in-progress" | "failure" | "entered-in-error") | undefined;
    }

    setStatus (value: ("success" | "data-requested" | "data-required" | "in-progress" | "failure" | "entered-in-error")) : this {
        Object.assign(this.resource, { status: value });
        return this;
    }

    getModuleUri () : string | undefined {
        return this.resource.moduleUri as string | undefined;
    }

    setModuleUri (value: string) : this {
        Object.assign(this.resource, { moduleUri: value });
        return this;
    }

    // Extensions
    public setCdsHooksEndpoint (value: cdsHooksEndpointProfile | Extension | string): this {
        if (value instanceof cdsHooksEndpointProfile) {
            pushExtension(this.resource, value.toResource())
        } else if (isExtension(value)) {
            if (value.url !== "http://hl7.org/fhir/StructureDefinition/cqf-cdsHooksEndpoint") throw new Error(`Expected extension url 'http://hl7.org/fhir/StructureDefinition/cqf-cdsHooksEndpoint', got '${value.url}'`)
            pushExtension(this.resource, value)
        } else {
            pushExtension(this.resource, cdsHooksEndpointProfile.createResource({ valueUri: value as string }))
        }
        return this
    }

    public getCdsHooksEndpoint(mode: 'flat'): string | undefined;
    public getCdsHooksEndpoint(mode: 'profile'): cdsHooksEndpointProfile | undefined;
    public getCdsHooksEndpoint(mode: 'raw'): Extension | undefined;
    public getCdsHooksEndpoint(): string | undefined;
    public getCdsHooksEndpoint (mode: 'flat' | 'profile' | 'raw' = 'flat'): string | cdsHooksEndpointProfile | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "http://hl7.org/fhir/StructureDefinition/cqf-cdsHooksEndpoint")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        if (mode === 'profile') return cdsHooksEndpointProfile.apply(ext)
        return getExtensionValue<string>(ext, "valueUri")
    }

    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "CDS Hooks GuidanceResponse"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "requestIdentifier"),
                ...validateRequired(res, profileName, "identifier"),
                ...validateReference(res, profileName, "subject", ["Group","Patient"]),
                ...validateReference(res, profileName, "performer", ["Device"]),
                ...validateReference(res, profileName, "result", ["CarePlan","RequestGroup"]),
            ],
            warnings: [
                ...validateMustSupport(res, profileName, "subject"),
                ...validateMustSupport(res, profileName, "occurrenceDateTime"),
                ...validateMustSupport(res, profileName, "performer"),
                ...validateMustSupport(res, profileName, "result"),
            ],
        }
    }

}

