// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Age } from "../../hl7-fhir-r4-core/Age";
import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type resolutionAgeProfileRaw = {
    valueAge: Age;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/allergyintolerance-resolutionAge (pkg: hl7.fhir.r4.core#4.0.1)
export class resolutionAgeProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/allergyintolerance-resolutionAge";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : resolutionAgeProfile {
        const profile = new resolutionAgeProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : resolutionAgeProfile {
        return new resolutionAgeProfile(resource);
    }

    static createResource (args: resolutionAgeProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/allergyintolerance-resolutionAge",
            valueAge: args.valueAge,
        })
        return resource;
    }

    static create (args: resolutionAgeProfileRaw) : resolutionAgeProfile {
        return resolutionAgeProfile.apply(resolutionAgeProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValueAge () : Age | undefined {
        return this.resource.valueAge as Age | undefined;
    }

    setValueAge (value: Age) : this {
        Object.assign(this.resource, { valueAge: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "resolutionAge"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", resolutionAgeProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valueAge"]),
            ],
            warnings: [],
        }
    }

}

