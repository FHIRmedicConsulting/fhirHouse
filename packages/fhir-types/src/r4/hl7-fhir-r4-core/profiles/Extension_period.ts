// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";
import type { Period } from "../../hl7-fhir-r4-core/Period";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type periodProfileRaw = {
    valuePeriod: Period;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/organization-period (pkg: hl7.fhir.r4.core#4.0.1)
export class periodProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/organization-period";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : periodProfile {
        const profile = new periodProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : periodProfile {
        return new periodProfile(resource);
    }

    static createResource (args: periodProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/organization-period",
            valuePeriod: args.valuePeriod,
        })
        return resource;
    }

    static create (args: periodProfileRaw) : periodProfile {
        return periodProfile.apply(periodProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValuePeriod () : Period | undefined {
        return this.resource.valuePeriod as Period | undefined;
    }

    setValuePeriod (value: Period) : this {
        Object.assign(this.resource, { valuePeriod: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "period"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", periodProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valuePeriod"]),
            ],
            warnings: [],
        }
    }

}

