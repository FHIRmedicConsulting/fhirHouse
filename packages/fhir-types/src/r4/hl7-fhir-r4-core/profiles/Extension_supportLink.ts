// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type supportLinkProfileRaw = {
    valueUri: string;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/questionnaire-supportLink (pkg: hl7.fhir.r4.core#4.0.1)
export class supportLinkProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/questionnaire-supportLink";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : supportLinkProfile {
        const profile = new supportLinkProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : supportLinkProfile {
        return new supportLinkProfile(resource);
    }

    static createResource (args: supportLinkProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/questionnaire-supportLink",
            valueUri: args.valueUri,
        })
        return resource;
    }

    static create (args: supportLinkProfileRaw) : supportLinkProfile {
        return supportLinkProfile.apply(supportLinkProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValueUri () : string | undefined {
        return this.resource.valueUri as string | undefined;
    }

    setValueUri (value: string) : this {
        Object.assign(this.resource, { valueUri: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "supportLink"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", supportLinkProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valueUri"]),
            ],
            warnings: [],
        }
    }

}

