// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Quantity } from "../../hl7-fhir-r4-core/Quantity";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/SimpleQuantity (pkg: hl7.fhir.r4.core#4.0.1)
export class SimpleQuantityProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/SimpleQuantity";

    private resource: Quantity;

    constructor (resource: Quantity) {
        this.resource = resource;
    }

    static from (resource: Quantity) : SimpleQuantityProfile {
        const profile = new SimpleQuantityProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Quantity) : SimpleQuantityProfile {
        return new SimpleQuantityProfile(resource);
    }

    static createResource () : Quantity {
        const resource = buildResource<Quantity>( {
        })
        return resource;
    }

    static create () : SimpleQuantityProfile {
        return SimpleQuantityProfile.apply(SimpleQuantityProfile.createResource());
    }

    toResource () : Quantity {
        return this.resource;
    }

    // Field accessors
    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "SimpleQuantity"
        const res = this.resource
        return {
            errors: [
                ...validateEnum(res, profileName, "comparator", ["<","<=",">=",">"]),
            ],
            warnings: [],
        }
    }

}

