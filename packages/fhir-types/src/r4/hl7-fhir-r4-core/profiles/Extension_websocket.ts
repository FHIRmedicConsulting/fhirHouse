// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type websocketProfileRaw = {
    valueUri: string;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/capabilitystatement-websocket (pkg: hl7.fhir.r4.core#4.0.1)
export class websocketProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/capabilitystatement-websocket";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : websocketProfile {
        const profile = new websocketProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : websocketProfile {
        return new websocketProfile(resource);
    }

    static createResource (args: websocketProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/capabilitystatement-websocket",
            valueUri: args.valueUri,
        })
        return resource;
    }

    static create (args: websocketProfileRaw) : websocketProfile {
        return websocketProfile.apply(websocketProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValueUri () : string | undefined {
        return this.resource.valueUri as string | undefined;
    }

    setValueUri (value: string) : this {
        Object.assign(this.resource, { valueUri: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "websocket"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", websocketProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valueUri"]),
            ],
            warnings: [],
        }
    }

}

