// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    isRawExtensionInput,
    isExtension,
    getExtensionValue,
    pushExtension,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type expand_groupProfileRaw = {
    extension?: Extension[];
}

export type expand_groupProfileFlat = {
    code?: string;
    display?: string;
    member: string[];
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/valueset-expand-group (pkg: hl7.fhir.r4.core#4.0.1)
export class expand_groupProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/valueset-expand-group";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : expand_groupProfile {
        const profile = new expand_groupProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : expand_groupProfile {
        return new expand_groupProfile(resource);
    }

    private static resolveInput (args: expand_groupProfileRaw | expand_groupProfileFlat) : Extension[] {
        if (isRawExtensionInput<expand_groupProfileRaw>(args)) {
            return args.extension ?? [];
        } else {
            const result: Extension[] = [];
            if (args.code !== undefined) {
                result.push({ url: "code", valueCode: args.code } as Extension);
            }
            if (args.display !== undefined) {
                result.push({ url: "display", valueString: args.display } as Extension);
            }
            if (args.member) {
                for (const item of args.member) {
                    result.push({ url: "member", valueCode: item } as Extension);
                }
            }
            return result;
        }
    }

    static createResource (args?: expand_groupProfileRaw | expand_groupProfileFlat) : Extension {
        const resolvedExtensions = expand_groupProfile.resolveInput(args ?? {});

        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/valueset-expand-group",
            extension: resolvedExtensions,
        })
        return resource;
    }

    static create (args?: expand_groupProfileRaw | expand_groupProfileFlat) : expand_groupProfile {
        return expand_groupProfile.apply(expand_groupProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    public setCode (value: string): this {
        pushExtension(this.resource, { url: "code", valueCode: value } as Extension)
        return this
    }

    public getCode(mode: 'flat'): string | undefined;
    public getCode(mode: 'raw'): Extension | undefined;
    public getCode(): string | undefined;
    public getCode (mode: 'flat' | 'raw' = 'flat'): string | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "code")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<string>(ext, "valueCode")
    }

    public setDisplay (value: string): this {
        pushExtension(this.resource, { url: "display", valueString: value } as Extension)
        return this
    }

    public getDisplay(mode: 'flat'): string | undefined;
    public getDisplay(mode: 'raw'): Extension | undefined;
    public getDisplay(): string | undefined;
    public getDisplay (mode: 'flat' | 'raw' = 'flat'): string | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "display")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<string>(ext, "valueString")
    }

    public setMember (value: string): this {
        pushExtension(this.resource, { url: "member", valueCode: value } as Extension)
        return this
    }

    public getMember(mode: 'flat'): string | undefined;
    public getMember(mode: 'raw'): Extension | undefined;
    public getMember(): string | undefined;
    public getMember (mode: 'flat' | 'raw' = 'flat'): string | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "member")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<string>(ext, "valueCode")
    }

    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "expand-group"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", expand_groupProfile.canonicalUrl),
            ],
            warnings: [],
        }
    }

}

