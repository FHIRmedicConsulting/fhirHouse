// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Identifier } from "../../hl7-fhir-r4-core/Identifier";
import type { RequestGroup } from "../../hl7-fhir-r4-core/RequestGroup";

import {
    buildResource,
    ensureProfile,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type CDS_Hooks_RequestGroupProfileRaw = {
    identifier: Identifier[];
    instantiatesUri: string[];
    status: ("draft" | "active" | "on-hold" | "revoked" | "completed" | "entered-in-error" | "unknown");
    intent: ("proposal" | "plan" | "directive" | "order" | "original-order" | "reflex-order" | "filler-order" | "instance-order" | "option");
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/cdshooksrequestgroup (pkg: hl7.fhir.r4.core#4.0.1)
export class CDS_Hooks_RequestGroupProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/cdshooksrequestgroup";

    private resource: RequestGroup;

    constructor (resource: RequestGroup) {
        this.resource = resource;
    }

    static from (resource: RequestGroup) : CDS_Hooks_RequestGroupProfile {
        if (!resource.meta?.profile?.includes(CDS_Hooks_RequestGroupProfile.canonicalUrl)) {
            throw new Error(`CDS_Hooks_RequestGroupProfile: meta.profile must include ${CDS_Hooks_RequestGroupProfile.canonicalUrl}`)
        }
        const profile = new CDS_Hooks_RequestGroupProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: RequestGroup) : CDS_Hooks_RequestGroupProfile {
        ensureProfile(resource, CDS_Hooks_RequestGroupProfile.canonicalUrl);
        return new CDS_Hooks_RequestGroupProfile(resource);
    }

    static createResource (args: CDS_Hooks_RequestGroupProfileRaw) : RequestGroup {
        const resource = buildResource<RequestGroup>( {
            resourceType: "RequestGroup",
            identifier: args.identifier,
            instantiatesUri: args.instantiatesUri,
            status: args.status,
            intent: args.intent,
            meta: { profile: [CDS_Hooks_RequestGroupProfile.canonicalUrl] },
        })
        return resource;
    }

    static create (args: CDS_Hooks_RequestGroupProfileRaw) : CDS_Hooks_RequestGroupProfile {
        return CDS_Hooks_RequestGroupProfile.apply(CDS_Hooks_RequestGroupProfile.createResource(args));
    }

    toResource () : RequestGroup {
        return this.resource;
    }

    // Field accessors
    getIdentifier () : Identifier[] | undefined {
        return this.resource.identifier as Identifier[] | undefined;
    }

    setIdentifier (value: Identifier[]) : this {
        Object.assign(this.resource, { identifier: value });
        return this;
    }

    getInstantiatesUri () : string[] | undefined {
        return this.resource.instantiatesUri as string[] | undefined;
    }

    setInstantiatesUri (value: string[]) : this {
        Object.assign(this.resource, { instantiatesUri: value });
        return this;
    }

    getStatus () : ("draft" | "active" | "on-hold" | "revoked" | "completed" | "entered-in-error" | "unknown") | undefined {
        return this.resource.status as ("draft" | "active" | "on-hold" | "revoked" | "completed" | "entered-in-error" | "unknown") | undefined;
    }

    setStatus (value: ("draft" | "active" | "on-hold" | "revoked" | "completed" | "entered-in-error" | "unknown")) : this {
        Object.assign(this.resource, { status: value });
        return this;
    }

    getIntent () : ("proposal" | "plan" | "directive" | "order" | "original-order" | "reflex-order" | "filler-order" | "instance-order" | "option") | undefined {
        return this.resource.intent as ("proposal" | "plan" | "directive" | "order" | "original-order" | "reflex-order" | "filler-order" | "instance-order" | "option") | undefined;
    }

    setIntent (value: ("proposal" | "plan" | "directive" | "order" | "original-order" | "reflex-order" | "filler-order" | "instance-order" | "option")) : this {
        Object.assign(this.resource, { intent: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "CDS Hooks RequestGroup"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "identifier"),
                ...validateRequired(res, profileName, "instantiatesUri"),
                ...validateEnum(res, profileName, "priority", ["routine","urgent","asap","stat"]),
                ...validateReference(res, profileName, "subject", ["Group","Patient"]),
                ...validateReference(res, profileName, "author", ["Device","Practitioner","PractitionerRole"]),
            ],
            warnings: [
                ...validateMustSupport(res, profileName, "priority"),
                ...validateMustSupport(res, profileName, "subject"),
                ...validateMustSupport(res, profileName, "authoredOn"),
                ...validateMustSupport(res, profileName, "author"),
            ],
        }
    }

}

