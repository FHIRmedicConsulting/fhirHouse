// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type Human_LanguageProfileRaw = {
    valueCode: string;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/language (pkg: hl7.fhir.r4.core#4.0.1)
export class Human_LanguageProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/language";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : Human_LanguageProfile {
        const profile = new Human_LanguageProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : Human_LanguageProfile {
        return new Human_LanguageProfile(resource);
    }

    static createResource (args: Human_LanguageProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/language",
            valueCode: args.valueCode,
        })
        return resource;
    }

    static create (args: Human_LanguageProfileRaw) : Human_LanguageProfile {
        return Human_LanguageProfile.apply(Human_LanguageProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValueCode () : string | undefined {
        return this.resource.valueCode as string | undefined;
    }

    setValueCode (value: string) : this {
        Object.assign(this.resource, { valueCode: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "Human Language"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", Human_LanguageProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valueCode"]),
            ],
            warnings: [],
        }
    }

}

