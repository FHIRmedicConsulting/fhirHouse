// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    isRawExtensionInput,
    isExtension,
    getExtensionValue,
    pushExtension,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type search_parameter_combinationProfileRaw = {
    extension: Extension[];
}

export type search_parameter_combinationProfileFlat = {
    required: string[];
    optional?: string[];
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/capabilitystatement-search-parameter-combination (pkg: hl7.fhir.r4.core#4.0.1)
export class search_parameter_combinationProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/capabilitystatement-search-parameter-combination";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : search_parameter_combinationProfile {
        const profile = new search_parameter_combinationProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : search_parameter_combinationProfile {
        return new search_parameter_combinationProfile(resource);
    }

    private static resolveInput (args: search_parameter_combinationProfileRaw | search_parameter_combinationProfileFlat) : Extension[] {
        if (isRawExtensionInput<search_parameter_combinationProfileRaw>(args)) {
            return args.extension ?? [];
        } else {
            const result: Extension[] = [];
            if (args.required) {
                for (const item of args.required) {
                    result.push({ url: "required", valueString: item } as Extension);
                }
            }
            if (args.optional) {
                for (const item of args.optional) {
                    result.push({ url: "optional", valueString: item } as Extension);
                }
            }
            return result;
        }
    }

    static createResource (args: search_parameter_combinationProfileRaw | search_parameter_combinationProfileFlat) : Extension {
        const resolvedExtensions = search_parameter_combinationProfile.resolveInput(args ?? {});

        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/capabilitystatement-search-parameter-combination",
            extension: resolvedExtensions,
        })
        return resource;
    }

    static create (args: search_parameter_combinationProfileRaw | search_parameter_combinationProfileFlat) : search_parameter_combinationProfile {
        return search_parameter_combinationProfile.apply(search_parameter_combinationProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getExtension () : Extension[] | undefined {
        return this.resource.extension as Extension[] | undefined;
    }

    setExtension (value: Extension[]) : this {
        Object.assign(this.resource, { extension: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    public setRequired (value: string): this {
        pushExtension(this.resource, { url: "required", valueString: value } as Extension)
        return this
    }

    public getRequired(mode: 'flat'): string | undefined;
    public getRequired(mode: 'raw'): Extension | undefined;
    public getRequired(): string | undefined;
    public getRequired (mode: 'flat' | 'raw' = 'flat'): string | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "required")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<string>(ext, "valueString")
    }

    public setOptional (value: string): this {
        pushExtension(this.resource, { url: "optional", valueString: value } as Extension)
        return this
    }

    public getOptional(mode: 'flat'): string | undefined;
    public getOptional(mode: 'raw'): Extension | undefined;
    public getOptional(): string | undefined;
    public getOptional (mode: 'flat' | 'raw' = 'flat'): string | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "optional")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<string>(ext, "valueString")
    }

    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "search-parameter-combination"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "extension"),
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", search_parameter_combinationProfile.canonicalUrl),
            ],
            warnings: [],
        }
    }

}

