// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";
import type { Reference } from "../../hl7-fhir-r4-core/Reference";

import {
    buildResource,
    isRawExtensionInput,
    isExtension,
    getExtensionValue,
    pushExtension,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type PhaseSetProfileRaw = {
    extension?: Extension[];
}

export type PhaseSetProfileFlat = {
    id?: string;
    molecularSequence: Reference[];
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/observation-geneticsPhaseSet (pkg: hl7.fhir.r4.core#4.0.1)
export class PhaseSetProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/observation-geneticsPhaseSet";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : PhaseSetProfile {
        const profile = new PhaseSetProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : PhaseSetProfile {
        return new PhaseSetProfile(resource);
    }

    private static resolveInput (args: PhaseSetProfileRaw | PhaseSetProfileFlat) : Extension[] {
        if (isRawExtensionInput<PhaseSetProfileRaw>(args)) {
            return args.extension ?? [];
        } else {
            const result: Extension[] = [];
            if (args.id !== undefined) {
                result.push({ url: "Id", valueUri: args.id } as Extension);
            }
            if (args.molecularSequence) {
                for (const item of args.molecularSequence) {
                    result.push({ url: "MolecularSequence", valueReference: item } as Extension);
                }
            }
            return result;
        }
    }

    static createResource (args?: PhaseSetProfileRaw | PhaseSetProfileFlat) : Extension {
        const resolvedExtensions = PhaseSetProfile.resolveInput(args ?? {});

        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/observation-geneticsPhaseSet",
            extension: resolvedExtensions,
        })
        return resource;
    }

    static create (args?: PhaseSetProfileRaw | PhaseSetProfileFlat) : PhaseSetProfile {
        return PhaseSetProfile.apply(PhaseSetProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    public setId (value: string): this {
        pushExtension(this.resource, { url: "Id", valueUri: value } as Extension)
        return this
    }

    public getId(mode: 'flat'): string | undefined;
    public getId(mode: 'raw'): Extension | undefined;
    public getId(): string | undefined;
    public getId (mode: 'flat' | 'raw' = 'flat'): string | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "Id")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<string>(ext, "valueUri")
    }

    public setMolecularSequence (value: Reference): this {
        pushExtension(this.resource, { url: "MolecularSequence", valueReference: value } as Extension)
        return this
    }

    public getMolecularSequence(mode: 'flat'): Reference | undefined;
    public getMolecularSequence(mode: 'raw'): Extension | undefined;
    public getMolecularSequence(): Reference | undefined;
    public getMolecularSequence (mode: 'flat' | 'raw' = 'flat'): Reference | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "MolecularSequence")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<Reference>(ext, "valueReference")
    }

    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "PhaseSet"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", PhaseSetProfile.canonicalUrl),
            ],
            warnings: [],
        }
    }

}

