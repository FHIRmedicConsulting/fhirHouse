// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type exposureDateProfileRaw = {
    valueDateTime: string;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/openEHR-exposureDate (pkg: hl7.fhir.r4.core#4.0.1)
export class exposureDateProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/openEHR-exposureDate";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : exposureDateProfile {
        const profile = new exposureDateProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : exposureDateProfile {
        return new exposureDateProfile(resource);
    }

    static createResource (args: exposureDateProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/openEHR-exposureDate",
            valueDateTime: args.valueDateTime,
        })
        return resource;
    }

    static create (args: exposureDateProfileRaw) : exposureDateProfile {
        return exposureDateProfile.apply(exposureDateProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValueDateTime () : string | undefined {
        return this.resource.valueDateTime as string | undefined;
    }

    setValueDateTime (value: string) : this {
        Object.assign(this.resource, { valueDateTime: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "exposureDate"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", exposureDateProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valueDateTime"]),
            ],
            warnings: [],
        }
    }

}

