// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    isRawExtensionInput,
    isExtension,
    getExtensionValue,
    pushExtension,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type usageProfileRaw = {
    extension: Extension[];
}

export type usageProfileFlat = {
    user: string;
    use: string;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/valueset-usage (pkg: hl7.fhir.r4.core#4.0.1)
export class usageProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/valueset-usage";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : usageProfile {
        const profile = new usageProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : usageProfile {
        return new usageProfile(resource);
    }

    private static resolveInput (args: usageProfileRaw | usageProfileFlat) : Extension[] {
        if (isRawExtensionInput<usageProfileRaw>(args)) {
            return args.extension ?? [];
        } else {
            const result: Extension[] = [];
            if (args.user !== undefined) {
                result.push({ url: "user", valueString: args.user } as Extension);
            }
            if (args.use !== undefined) {
                result.push({ url: "use", valueString: args.use } as Extension);
            }
            return result;
        }
    }

    static createResource (args: usageProfileRaw | usageProfileFlat) : Extension {
        const resolvedExtensions = usageProfile.resolveInput(args ?? {});

        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/valueset-usage",
            extension: resolvedExtensions,
        })
        return resource;
    }

    static create (args: usageProfileRaw | usageProfileFlat) : usageProfile {
        return usageProfile.apply(usageProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getExtension () : Extension[] | undefined {
        return this.resource.extension as Extension[] | undefined;
    }

    setExtension (value: Extension[]) : this {
        Object.assign(this.resource, { extension: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    public setUser (value: string): this {
        pushExtension(this.resource, { url: "user", valueString: value } as Extension)
        return this
    }

    public getUser(mode: 'flat'): string | undefined;
    public getUser(mode: 'raw'): Extension | undefined;
    public getUser(): string | undefined;
    public getUser (mode: 'flat' | 'raw' = 'flat'): string | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "user")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<string>(ext, "valueString")
    }

    public setUse (value: string): this {
        pushExtension(this.resource, { url: "use", valueString: value } as Extension)
        return this
    }

    public getUse(mode: 'flat'): string | undefined;
    public getUse(mode: 'raw'): Extension | undefined;
    public getUse(): string | undefined;
    public getUse (mode: 'flat' | 'raw' = 'flat'): string | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "use")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<string>(ext, "valueString")
    }

    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "usage"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "extension"),
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", usageProfile.canonicalUrl),
            ],
            warnings: [],
        }
    }

}

