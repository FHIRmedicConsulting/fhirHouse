// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    isRawExtensionInput,
    isExtension,
    getExtensionValue,
    pushExtension,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type daysOfCycleProfileRaw = {
    extension: Extension[];
}

export type daysOfCycleProfileFlat = {
    day: number[];
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/timing-daysOfCycle (pkg: hl7.fhir.r4.core#4.0.1)
export class daysOfCycleProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/timing-daysOfCycle";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : daysOfCycleProfile {
        const profile = new daysOfCycleProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : daysOfCycleProfile {
        return new daysOfCycleProfile(resource);
    }

    private static resolveInput (args: daysOfCycleProfileRaw | daysOfCycleProfileFlat) : Extension[] {
        if (isRawExtensionInput<daysOfCycleProfileRaw>(args)) {
            return args.extension ?? [];
        } else {
            const result: Extension[] = [];
            if (args.day) {
                for (const item of args.day) {
                    result.push({ url: "day", valueInteger: item } as Extension);
                }
            }
            return result;
        }
    }

    static createResource (args: daysOfCycleProfileRaw | daysOfCycleProfileFlat) : Extension {
        const resolvedExtensions = daysOfCycleProfile.resolveInput(args ?? {});

        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/timing-daysOfCycle",
            extension: resolvedExtensions,
        })
        return resource;
    }

    static create (args: daysOfCycleProfileRaw | daysOfCycleProfileFlat) : daysOfCycleProfile {
        return daysOfCycleProfile.apply(daysOfCycleProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getExtension () : Extension[] | undefined {
        return this.resource.extension as Extension[] | undefined;
    }

    setExtension (value: Extension[]) : this {
        Object.assign(this.resource, { extension: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    public setDay (value: number): this {
        pushExtension(this.resource, { url: "day", valueInteger: value } as Extension)
        return this
    }

    public getDay(mode: 'flat'): number | undefined;
    public getDay(mode: 'raw'): Extension | undefined;
    public getDay(): number | undefined;
    public getDay (mode: 'flat' | 'raw' = 'flat'): number | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "day")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<number>(ext, "valueInteger")
    }

    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "daysOfCycle"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "extension"),
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", daysOfCycleProfile.canonicalUrl),
            ],
            warnings: [],
        }
    }

}

