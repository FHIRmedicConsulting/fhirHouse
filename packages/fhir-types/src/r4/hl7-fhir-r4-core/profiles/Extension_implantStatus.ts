// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type implantStatusProfileRaw = {
    valueCode: string;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/device-implantStatus (pkg: hl7.fhir.r4.core#4.0.1)
export class implantStatusProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/device-implantStatus";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : implantStatusProfile {
        const profile = new implantStatusProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : implantStatusProfile {
        return new implantStatusProfile(resource);
    }

    static createResource (args: implantStatusProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/device-implantStatus",
            valueCode: args.valueCode,
        })
        return resource;
    }

    static create (args: implantStatusProfileRaw) : implantStatusProfile {
        return implantStatusProfile.apply(implantStatusProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValueCode () : string | undefined {
        return this.resource.valueCode as string | undefined;
    }

    setValueCode (value: string) : this {
        Object.assign(this.resource, { valueCode: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "implantStatus"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", implantStatusProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valueCode"]),
            ],
            warnings: [],
        }
    }

}

