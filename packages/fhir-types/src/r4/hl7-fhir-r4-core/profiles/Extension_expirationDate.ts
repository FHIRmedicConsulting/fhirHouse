// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type expirationDateProfileRaw = {
    valueDate: string;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/valueset-expirationDate (pkg: hl7.fhir.r4.core#4.0.1)
export class expirationDateProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/valueset-expirationDate";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : expirationDateProfile {
        const profile = new expirationDateProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : expirationDateProfile {
        return new expirationDateProfile(resource);
    }

    static createResource (args: expirationDateProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/valueset-expirationDate",
            valueDate: args.valueDate,
        })
        return resource;
    }

    static create (args: expirationDateProfileRaw) : expirationDateProfile {
        return expirationDateProfile.apply(expirationDateProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValueDate () : string | undefined {
        return this.resource.valueDate as string | undefined;
    }

    setValueDate (value: string) : this {
        Object.assign(this.resource, { valueDate: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "expirationDate"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", expirationDateProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valueDate"]),
            ],
            warnings: [],
        }
    }

}

