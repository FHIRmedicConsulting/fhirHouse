// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { CodeableConcept } from "../../hl7-fhir-r4-core/CodeableConcept";
import type { DiagnosticReport } from "../../hl7-fhir-r4-core/DiagnosticReport";
import type { Extension } from "../../hl7-fhir-r4-core/Extension";
import type { Reference } from "../../hl7-fhir-r4-core/Reference";

import { AnalysisProfile, type AnalysisProfileFlat } from "./Extension_Analysis";
import { AssessedConditionProfile } from "./Extension_AssessedCondition";
import { FamilyMemberHistoryProfile } from "./Extension_FamilyMemberHistory";
import { ReferencesProfile, type ReferencesProfileFlat } from "./Extension_References";

import {
    buildResource,
    ensureProfile,
    extractComplexExtension,
    isExtension,
    getExtensionValue,
    pushExtension,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type DiagnosticReport_GeneticsProfileRaw = {
    status: ("registered" | "partial" | "preliminary" | "final" | "amended" | "corrected" | "appended" | "cancelled" | "entered-in-error" | "unknown");
    code: CodeableConcept;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/diagnosticreport-genetics (pkg: hl7.fhir.r4.core#4.0.1)
export class DiagnosticReport_GeneticsProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/diagnosticreport-genetics";

    private resource: DiagnosticReport;

    constructor (resource: DiagnosticReport) {
        this.resource = resource;
    }

    static from (resource: DiagnosticReport) : DiagnosticReport_GeneticsProfile {
        if (!resource.meta?.profile?.includes(DiagnosticReport_GeneticsProfile.canonicalUrl)) {
            throw new Error(`DiagnosticReport_GeneticsProfile: meta.profile must include ${DiagnosticReport_GeneticsProfile.canonicalUrl}`)
        }
        const profile = new DiagnosticReport_GeneticsProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: DiagnosticReport) : DiagnosticReport_GeneticsProfile {
        ensureProfile(resource, DiagnosticReport_GeneticsProfile.canonicalUrl);
        return new DiagnosticReport_GeneticsProfile(resource);
    }

    static createResource (args: DiagnosticReport_GeneticsProfileRaw) : DiagnosticReport {
        const resource = buildResource<DiagnosticReport>( {
            resourceType: "DiagnosticReport",
            status: args.status,
            code: args.code,
            meta: { profile: [DiagnosticReport_GeneticsProfile.canonicalUrl] },
        })
        return resource;
    }

    static create (args: DiagnosticReport_GeneticsProfileRaw) : DiagnosticReport_GeneticsProfile {
        return DiagnosticReport_GeneticsProfile.apply(DiagnosticReport_GeneticsProfile.createResource(args));
    }

    toResource () : DiagnosticReport {
        return this.resource;
    }

    // Field accessors
    getStatus () : ("registered" | "partial" | "preliminary" | "final" | "amended" | "corrected" | "appended" | "cancelled" | "entered-in-error" | "unknown") | undefined {
        return this.resource.status as ("registered" | "partial" | "preliminary" | "final" | "amended" | "corrected" | "appended" | "cancelled" | "entered-in-error" | "unknown") | undefined;
    }

    setStatus (value: ("registered" | "partial" | "preliminary" | "final" | "amended" | "corrected" | "appended" | "cancelled" | "entered-in-error" | "unknown")) : this {
        Object.assign(this.resource, { status: value });
        return this;
    }

    getCode () : CodeableConcept | undefined {
        return this.resource.code as CodeableConcept | undefined;
    }

    setCode (value: CodeableConcept) : this {
        Object.assign(this.resource, { code: value });
        return this;
    }

    // Extensions
    public setAssessedCondition (value: AssessedConditionProfile | Extension | Reference): this {
        if (value instanceof AssessedConditionProfile) {
            pushExtension(this.resource, value.toResource())
        } else if (isExtension(value)) {
            if (value.url !== "http://hl7.org/fhir/StructureDefinition/DiagnosticReport-geneticsAssessedCondition") throw new Error(`Expected extension url 'http://hl7.org/fhir/StructureDefinition/DiagnosticReport-geneticsAssessedCondition', got '${value.url}'`)
            pushExtension(this.resource, value)
        } else {
            pushExtension(this.resource, AssessedConditionProfile.createResource({ valueReference: value as Reference }))
        }
        return this
    }

    public getAssessedCondition(mode: 'flat'): Reference | undefined;
    public getAssessedCondition(mode: 'profile'): AssessedConditionProfile | undefined;
    public getAssessedCondition(mode: 'raw'): Extension | undefined;
    public getAssessedCondition(): Reference | undefined;
    public getAssessedCondition (mode: 'flat' | 'profile' | 'raw' = 'flat'): Reference | AssessedConditionProfile | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "http://hl7.org/fhir/StructureDefinition/DiagnosticReport-geneticsAssessedCondition")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        if (mode === 'profile') return AssessedConditionProfile.apply(ext)
        return getExtensionValue<Reference>(ext, "valueReference")
    }

    public setFamilyMemberHistory (value: FamilyMemberHistoryProfile | Extension | Reference): this {
        if (value instanceof FamilyMemberHistoryProfile) {
            pushExtension(this.resource, value.toResource())
        } else if (isExtension(value)) {
            if (value.url !== "http://hl7.org/fhir/StructureDefinition/DiagnosticReport-geneticsFamilyMemberHistory") throw new Error(`Expected extension url 'http://hl7.org/fhir/StructureDefinition/DiagnosticReport-geneticsFamilyMemberHistory', got '${value.url}'`)
            pushExtension(this.resource, value)
        } else {
            pushExtension(this.resource, FamilyMemberHistoryProfile.createResource({ valueReference: value as Reference }))
        }
        return this
    }

    public getFamilyMemberHistory(mode: 'flat'): Reference | undefined;
    public getFamilyMemberHistory(mode: 'profile'): FamilyMemberHistoryProfile | undefined;
    public getFamilyMemberHistory(mode: 'raw'): Extension | undefined;
    public getFamilyMemberHistory(): Reference | undefined;
    public getFamilyMemberHistory (mode: 'flat' | 'profile' | 'raw' = 'flat'): Reference | FamilyMemberHistoryProfile | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "http://hl7.org/fhir/StructureDefinition/DiagnosticReport-geneticsFamilyMemberHistory")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        if (mode === 'profile') return FamilyMemberHistoryProfile.apply(ext)
        return getExtensionValue<Reference>(ext, "valueReference")
    }

    public setAnalysis (input: AnalysisProfileFlat | AnalysisProfile | Extension): this {
        if (input instanceof AnalysisProfile) {
            pushExtension(this.resource, input.toResource())
        } else if (isExtension<Extension>(input)) {
            if (input.url !== "http://hl7.org/fhir/StructureDefinition/DiagnosticReport-geneticsAnalysis") throw new Error(`Expected extension url 'http://hl7.org/fhir/StructureDefinition/DiagnosticReport-geneticsAnalysis', got '${input.url}'`)
            pushExtension(this.resource, input)
        } else {
            pushExtension(this.resource, AnalysisProfile.createResource(input))
        }
        return this
    }

    public getAnalysis(mode: 'flat'): AnalysisProfileFlat | undefined;
    public getAnalysis(mode: 'profile'): AnalysisProfile | undefined;
    public getAnalysis(mode: 'raw'): Extension | undefined;
    public getAnalysis(): AnalysisProfileFlat | undefined;
    public getAnalysis (mode: 'flat' | 'profile' | 'raw' = 'flat'): AnalysisProfileFlat | AnalysisProfile | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "http://hl7.org/fhir/StructureDefinition/DiagnosticReport-geneticsAnalysis")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        if (mode === 'profile') return AnalysisProfile.apply(ext)
        const config = [{ name: "type", valueField: "valueCodeableConcept", isArray: false }, { name: "interpretation", valueField: "valueCodeableConcept", isArray: false }]
        return extractComplexExtension<AnalysisProfileFlat>(ext, config)
    }

    public setReferences (input: ReferencesProfileFlat | ReferencesProfile | Extension): this {
        if (input instanceof ReferencesProfile) {
            pushExtension(this.resource, input.toResource())
        } else if (isExtension<Extension>(input)) {
            if (input.url !== "http://hl7.org/fhir/StructureDefinition/DiagnosticReport-geneticsReferences") throw new Error(`Expected extension url 'http://hl7.org/fhir/StructureDefinition/DiagnosticReport-geneticsReferences', got '${input.url}'`)
            pushExtension(this.resource, input)
        } else {
            pushExtension(this.resource, ReferencesProfile.createResource(input))
        }
        return this
    }

    public getReferences(mode: 'flat'): ReferencesProfileFlat | undefined;
    public getReferences(mode: 'profile'): ReferencesProfile | undefined;
    public getReferences(mode: 'raw'): Extension | undefined;
    public getReferences(): ReferencesProfileFlat | undefined;
    public getReferences (mode: 'flat' | 'profile' | 'raw' = 'flat'): ReferencesProfileFlat | ReferencesProfile | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "http://hl7.org/fhir/StructureDefinition/DiagnosticReport-geneticsReferences")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        if (mode === 'profile') return ReferencesProfile.apply(ext)
        const config = [{ name: "description", valueField: "valueString", isArray: false }, { name: "reference", valueField: "valueUri", isArray: true }, { name: "type", valueField: "valueCodeableConcept", isArray: false }]
        return extractComplexExtension<ReferencesProfileFlat>(ext, config)
    }

    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "DiagnosticReport-Genetics"
        const res = this.resource
        return {
            errors: [],
            warnings: [],
        }
    }

}

