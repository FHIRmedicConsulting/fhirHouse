// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";
import type { Signature } from "../../hl7-fhir-r4-core/Signature";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type signatureProfileRaw = {
    valueSignature: Signature;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/questionnaireresponse-signature (pkg: hl7.fhir.r4.core#4.0.1)
export class signatureProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/questionnaireresponse-signature";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : signatureProfile {
        const profile = new signatureProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : signatureProfile {
        return new signatureProfile(resource);
    }

    static createResource (args: signatureProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/questionnaireresponse-signature",
            valueSignature: args.valueSignature,
        })
        return resource;
    }

    static create (args: signatureProfileRaw) : signatureProfile {
        return signatureProfile.apply(signatureProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValueSignature () : Signature | undefined {
        return this.resource.valueSignature as Signature | undefined;
    }

    setValueSignature (value: Signature) : this {
        Object.assign(this.resource, { valueSignature: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "signature"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", signatureProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valueSignature"]),
            ],
            warnings: [],
        }
    }

}

