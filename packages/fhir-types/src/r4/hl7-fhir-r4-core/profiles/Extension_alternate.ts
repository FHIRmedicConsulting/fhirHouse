// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Coding } from "../../hl7-fhir-r4-core/Coding";
import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    isRawExtensionInput,
    isExtension,
    getExtensionValue,
    pushExtension,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type alternateProfileRaw = {
    extension: Extension[];
}

export type alternateProfileFlat = {
    code: string;
    use: Coding;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/codesystem-alternate (pkg: hl7.fhir.r4.core#4.0.1)
export class alternateProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/codesystem-alternate";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : alternateProfile {
        const profile = new alternateProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : alternateProfile {
        return new alternateProfile(resource);
    }

    private static resolveInput (args: alternateProfileRaw | alternateProfileFlat) : Extension[] {
        if (isRawExtensionInput<alternateProfileRaw>(args)) {
            return args.extension ?? [];
        } else {
            const result: Extension[] = [];
            if (args.code !== undefined) {
                result.push({ url: "code", valueCode: args.code } as Extension);
            }
            if (args.use !== undefined) {
                result.push({ url: "use", valueCoding: args.use } as Extension);
            }
            return result;
        }
    }

    static createResource (args: alternateProfileRaw | alternateProfileFlat) : Extension {
        const resolvedExtensions = alternateProfile.resolveInput(args ?? {});

        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/codesystem-alternate",
            extension: resolvedExtensions,
        })
        return resource;
    }

    static create (args: alternateProfileRaw | alternateProfileFlat) : alternateProfile {
        return alternateProfile.apply(alternateProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getExtension () : Extension[] | undefined {
        return this.resource.extension as Extension[] | undefined;
    }

    setExtension (value: Extension[]) : this {
        Object.assign(this.resource, { extension: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    public setCode (value: string): this {
        pushExtension(this.resource, { url: "code", valueCode: value } as Extension)
        return this
    }

    public getCode(mode: 'flat'): string | undefined;
    public getCode(mode: 'raw'): Extension | undefined;
    public getCode(): string | undefined;
    public getCode (mode: 'flat' | 'raw' = 'flat'): string | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "code")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<string>(ext, "valueCode")
    }

    public setUse (value: Coding): this {
        pushExtension(this.resource, { url: "use", valueCoding: value } as Extension)
        return this
    }

    public getUse(mode: 'flat'): Coding | undefined;
    public getUse(mode: 'raw'): Extension | undefined;
    public getUse(): Coding | undefined;
    public getUse (mode: 'flat' | 'raw' = 'flat'): Coding | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "use")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<Coding>(ext, "valueCoding")
    }

    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "alternate"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "extension"),
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", alternateProfile.canonicalUrl),
            ],
            warnings: [],
        }
    }

}

