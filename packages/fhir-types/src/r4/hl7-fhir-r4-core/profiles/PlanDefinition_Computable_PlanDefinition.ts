// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { PlanDefinition } from "../../hl7-fhir-r4-core/PlanDefinition";

import {
    buildResource,
    ensureProfile,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type Computable_PlanDefinitionProfileRaw = {
    library: string[];
    status: ("draft" | "active" | "retired" | "unknown");
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/computableplandefinition (pkg: hl7.fhir.r4.core#4.0.1)
export class Computable_PlanDefinitionProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/computableplandefinition";

    private resource: PlanDefinition;

    constructor (resource: PlanDefinition) {
        this.resource = resource;
    }

    static from (resource: PlanDefinition) : Computable_PlanDefinitionProfile {
        if (!resource.meta?.profile?.includes(Computable_PlanDefinitionProfile.canonicalUrl)) {
            throw new Error(`Computable_PlanDefinitionProfile: meta.profile must include ${Computable_PlanDefinitionProfile.canonicalUrl}`)
        }
        const profile = new Computable_PlanDefinitionProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: PlanDefinition) : Computable_PlanDefinitionProfile {
        ensureProfile(resource, Computable_PlanDefinitionProfile.canonicalUrl);
        return new Computable_PlanDefinitionProfile(resource);
    }

    static createResource (args: Computable_PlanDefinitionProfileRaw) : PlanDefinition {
        const resource = buildResource<PlanDefinition>( {
            resourceType: "PlanDefinition",
            library: args.library,
            status: args.status,
            meta: { profile: [Computable_PlanDefinitionProfile.canonicalUrl] },
        })
        return resource;
    }

    static create (args: Computable_PlanDefinitionProfileRaw) : Computable_PlanDefinitionProfile {
        return Computable_PlanDefinitionProfile.apply(Computable_PlanDefinitionProfile.createResource(args));
    }

    toResource () : PlanDefinition {
        return this.resource;
    }

    // Field accessors
    getLibrary () : string[] | undefined {
        return this.resource.library as string[] | undefined;
    }

    setLibrary (value: string[]) : this {
        Object.assign(this.resource, { library: value });
        return this;
    }

    getStatus () : ("draft" | "active" | "retired" | "unknown") | undefined {
        return this.resource.status as ("draft" | "active" | "retired" | "unknown") | undefined;
    }

    setStatus (value: ("draft" | "active" | "retired" | "unknown")) : this {
        Object.assign(this.resource, { status: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "Computable PlanDefinition"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "library"),
            ],
            warnings: [],
        }
    }

}

