// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Duration } from "../../hl7-fhir-r4-core/Duration";
import type { Extension } from "../../hl7-fhir-r4-core/Extension";
import type { Reference } from "../../hl7-fhir-r4-core/Reference";

import {
    buildResource,
    isRawExtensionInput,
    isExtension,
    getExtensionValue,
    pushExtension,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type relativeDateTimeProfileRaw = {
    extension: Extension[];
}

export type relativeDateTimeProfileFlat = {
    target: Reference;
    targetPath: string;
    relationship: string;
    offset: Duration;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/cqf-relativeDateTime (pkg: hl7.fhir.r4.core#4.0.1)
export class relativeDateTimeProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/cqf-relativeDateTime";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : relativeDateTimeProfile {
        const profile = new relativeDateTimeProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : relativeDateTimeProfile {
        return new relativeDateTimeProfile(resource);
    }

    private static resolveInput (args: relativeDateTimeProfileRaw | relativeDateTimeProfileFlat) : Extension[] {
        if (isRawExtensionInput<relativeDateTimeProfileRaw>(args)) {
            return args.extension ?? [];
        } else {
            const result: Extension[] = [];
            if (args.target !== undefined) {
                result.push({ url: "target", valueReference: args.target } as Extension);
            }
            if (args.targetPath !== undefined) {
                result.push({ url: "targetPath", valueString: args.targetPath } as Extension);
            }
            if (args.relationship !== undefined) {
                result.push({ url: "relationship", valueCode: args.relationship } as Extension);
            }
            if (args.offset !== undefined) {
                result.push({ url: "offset", valueDuration: args.offset } as Extension);
            }
            return result;
        }
    }

    static createResource (args: relativeDateTimeProfileRaw | relativeDateTimeProfileFlat) : Extension {
        const resolvedExtensions = relativeDateTimeProfile.resolveInput(args ?? {});

        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/cqf-relativeDateTime",
            extension: resolvedExtensions,
        })
        return resource;
    }

    static create (args: relativeDateTimeProfileRaw | relativeDateTimeProfileFlat) : relativeDateTimeProfile {
        return relativeDateTimeProfile.apply(relativeDateTimeProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getExtension () : Extension[] | undefined {
        return this.resource.extension as Extension[] | undefined;
    }

    setExtension (value: Extension[]) : this {
        Object.assign(this.resource, { extension: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    public setTarget (value: Reference): this {
        pushExtension(this.resource, { url: "target", valueReference: value } as Extension)
        return this
    }

    public getTarget(mode: 'flat'): Reference | undefined;
    public getTarget(mode: 'raw'): Extension | undefined;
    public getTarget(): Reference | undefined;
    public getTarget (mode: 'flat' | 'raw' = 'flat'): Reference | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "target")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<Reference>(ext, "valueReference")
    }

    public setTargetPath (value: string): this {
        pushExtension(this.resource, { url: "targetPath", valueString: value } as Extension)
        return this
    }

    public getTargetPath(mode: 'flat'): string | undefined;
    public getTargetPath(mode: 'raw'): Extension | undefined;
    public getTargetPath(): string | undefined;
    public getTargetPath (mode: 'flat' | 'raw' = 'flat'): string | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "targetPath")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<string>(ext, "valueString")
    }

    public setRelationship (value: string): this {
        pushExtension(this.resource, { url: "relationship", valueCode: value } as Extension)
        return this
    }

    public getRelationship(mode: 'flat'): string | undefined;
    public getRelationship(mode: 'raw'): Extension | undefined;
    public getRelationship(): string | undefined;
    public getRelationship (mode: 'flat' | 'raw' = 'flat'): string | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "relationship")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<string>(ext, "valueCode")
    }

    public setOffset (value: Duration): this {
        pushExtension(this.resource, { url: "offset", valueDuration: value } as Extension)
        return this
    }

    public getOffset(mode: 'flat'): Duration | undefined;
    public getOffset(mode: 'raw'): Extension | undefined;
    public getOffset(): Duration | undefined;
    public getOffset (mode: 'flat' | 'raw' = 'flat'): Duration | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "offset")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<Duration>(ext, "valueDuration")
    }

    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "relativeDateTime"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "extension"),
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", relativeDateTimeProfile.canonicalUrl),
            ],
            warnings: [],
        }
    }

}

