// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { EvidenceVariable, EvidenceVariableCharacteristic } from "../../hl7-fhir-r4-core/EvidenceVariable";

import {
    buildResource,
    ensureProfile,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type PICO_Element_ProfileRaw = {
    characteristic: EvidenceVariableCharacteristic[];
    status: ("draft" | "active" | "retired" | "unknown");
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/picoelement (pkg: hl7.fhir.r4.core#4.0.1)
export class PICO_Element_Profile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/picoelement";

    private resource: EvidenceVariable;

    constructor (resource: EvidenceVariable) {
        this.resource = resource;
    }

    static from (resource: EvidenceVariable) : PICO_Element_Profile {
        if (!resource.meta?.profile?.includes(PICO_Element_Profile.canonicalUrl)) {
            throw new Error(`PICO_Element_Profile: meta.profile must include ${PICO_Element_Profile.canonicalUrl}`)
        }
        const profile = new PICO_Element_Profile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: EvidenceVariable) : PICO_Element_Profile {
        ensureProfile(resource, PICO_Element_Profile.canonicalUrl);
        return new PICO_Element_Profile(resource);
    }

    static createResource (args: PICO_Element_ProfileRaw) : EvidenceVariable {
        const resource = buildResource<EvidenceVariable>( {
            resourceType: "EvidenceVariable",
            characteristic: args.characteristic,
            status: args.status,
            meta: { profile: [PICO_Element_Profile.canonicalUrl] },
        })
        return resource;
    }

    static create (args: PICO_Element_ProfileRaw) : PICO_Element_Profile {
        return PICO_Element_Profile.apply(PICO_Element_Profile.createResource(args));
    }

    toResource () : EvidenceVariable {
        return this.resource;
    }

    // Field accessors
    getCharacteristic () : EvidenceVariableCharacteristic[] | undefined {
        return this.resource.characteristic as EvidenceVariableCharacteristic[] | undefined;
    }

    setCharacteristic (value: EvidenceVariableCharacteristic[]) : this {
        Object.assign(this.resource, { characteristic: value });
        return this;
    }

    getStatus () : ("draft" | "active" | "retired" | "unknown") | undefined {
        return this.resource.status as ("draft" | "active" | "retired" | "unknown") | undefined;
    }

    setStatus (value: ("draft" | "active" | "retired" | "unknown")) : this {
        Object.assign(this.resource, { status: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "PICO Element Profile"
        const res = this.resource
        return {
            errors: [
                ...validateEnum(res, profileName, "type", ["dichotomous","continuous","descriptive"]),
                ...validateRequired(res, profileName, "characteristic"),
            ],
            warnings: [],
        }
    }

}

