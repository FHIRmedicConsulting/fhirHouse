// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type precisionProfileRaw = {
    valueInteger: number;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/quantity-precision (pkg: hl7.fhir.r4.core#4.0.1)
export class precisionProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/quantity-precision";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : precisionProfile {
        const profile = new precisionProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : precisionProfile {
        return new precisionProfile(resource);
    }

    static createResource (args: precisionProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/quantity-precision",
            valueInteger: args.valueInteger,
        })
        return resource;
    }

    static create (args: precisionProfileRaw) : precisionProfile {
        return precisionProfile.apply(precisionProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValueInteger () : number | undefined {
        return this.resource.valueInteger as number | undefined;
    }

    setValueInteger (value: number) : this {
        Object.assign(this.resource, { valueInteger: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "precision"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", precisionProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valueInteger"]),
            ],
            warnings: [],
        }
    }

}

