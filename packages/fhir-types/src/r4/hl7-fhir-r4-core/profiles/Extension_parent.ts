// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { CodeableConcept } from "../../hl7-fhir-r4-core/CodeableConcept";
import type { Extension } from "../../hl7-fhir-r4-core/Extension";
import type { Reference } from "../../hl7-fhir-r4-core/Reference";

import {
    buildResource,
    isRawExtensionInput,
    isExtension,
    getExtensionValue,
    pushExtension,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type parentProfileRaw = {
    extension: Extension[];
}

export type parentProfileFlat = {
    type: CodeableConcept;
    reference: Reference;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/family-member-history-genetics-parent (pkg: hl7.fhir.r4.core#4.0.1)
export class parentProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/family-member-history-genetics-parent";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : parentProfile {
        const profile = new parentProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : parentProfile {
        return new parentProfile(resource);
    }

    private static resolveInput (args: parentProfileRaw | parentProfileFlat) : Extension[] {
        if (isRawExtensionInput<parentProfileRaw>(args)) {
            return args.extension ?? [];
        } else {
            const result: Extension[] = [];
            if (args.type !== undefined) {
                result.push({ url: "type", valueCodeableConcept: args.type } as Extension);
            }
            if (args.reference !== undefined) {
                result.push({ url: "reference", valueReference: args.reference } as Extension);
            }
            return result;
        }
    }

    static createResource (args: parentProfileRaw | parentProfileFlat) : Extension {
        const resolvedExtensions = parentProfile.resolveInput(args ?? {});

        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/family-member-history-genetics-parent",
            extension: resolvedExtensions,
        })
        return resource;
    }

    static create (args: parentProfileRaw | parentProfileFlat) : parentProfile {
        return parentProfile.apply(parentProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getExtension () : Extension[] | undefined {
        return this.resource.extension as Extension[] | undefined;
    }

    setExtension (value: Extension[]) : this {
        Object.assign(this.resource, { extension: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    public setType (value: CodeableConcept): this {
        pushExtension(this.resource, { url: "type", valueCodeableConcept: value } as Extension)
        return this
    }

    public getType(mode: 'flat'): CodeableConcept | undefined;
    public getType(mode: 'raw'): Extension | undefined;
    public getType(): CodeableConcept | undefined;
    public getType (mode: 'flat' | 'raw' = 'flat'): CodeableConcept | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "type")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<CodeableConcept>(ext, "valueCodeableConcept")
    }

    public setReference (value: Reference): this {
        pushExtension(this.resource, { url: "reference", valueReference: value } as Extension)
        return this
    }

    public getReference(mode: 'flat'): Reference | undefined;
    public getReference(mode: 'raw'): Extension | undefined;
    public getReference(): Reference | undefined;
    public getReference (mode: 'flat' | 'raw' = 'flat'): Reference | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "reference")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<Reference>(ext, "valueReference")
    }

    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "parent"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "extension"),
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", parentProfile.canonicalUrl),
            ],
            warnings: [],
        }
    }

}

