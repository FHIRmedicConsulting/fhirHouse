// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Evidence } from "../../hl7-fhir-r4-core/Evidence";
import type { Reference } from "../../hl7-fhir-r4-core/Reference";

import {
    buildResource,
    ensureProfile,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type Evidence_Synthesis_ProfileRaw = {
    status: ("draft" | "active" | "retired" | "unknown");
    exposureBackground: Reference<"EvidenceVariable">;
    exposureVariant: Reference<"EvidenceVariable">[];
    outcome: Reference<"EvidenceVariable">[];
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/synthesis (pkg: hl7.fhir.r4.core#4.0.1)
export class Evidence_Synthesis_Profile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/synthesis";

    private resource: Evidence;

    constructor (resource: Evidence) {
        this.resource = resource;
    }

    static from (resource: Evidence) : Evidence_Synthesis_Profile {
        if (!resource.meta?.profile?.includes(Evidence_Synthesis_Profile.canonicalUrl)) {
            throw new Error(`Evidence_Synthesis_Profile: meta.profile must include ${Evidence_Synthesis_Profile.canonicalUrl}`)
        }
        const profile = new Evidence_Synthesis_Profile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Evidence) : Evidence_Synthesis_Profile {
        ensureProfile(resource, Evidence_Synthesis_Profile.canonicalUrl);
        return new Evidence_Synthesis_Profile(resource);
    }

    static createResource (args: Evidence_Synthesis_ProfileRaw) : Evidence {
        const resource = buildResource<Evidence>( {
            resourceType: "Evidence",
            status: args.status,
            exposureBackground: args.exposureBackground,
            exposureVariant: args.exposureVariant,
            outcome: args.outcome,
            meta: { profile: [Evidence_Synthesis_Profile.canonicalUrl] },
        })
        return resource;
    }

    static create (args: Evidence_Synthesis_ProfileRaw) : Evidence_Synthesis_Profile {
        return Evidence_Synthesis_Profile.apply(Evidence_Synthesis_Profile.createResource(args));
    }

    toResource () : Evidence {
        return this.resource;
    }

    // Field accessors
    getStatus () : ("draft" | "active" | "retired" | "unknown") | undefined {
        return this.resource.status as ("draft" | "active" | "retired" | "unknown") | undefined;
    }

    setStatus (value: ("draft" | "active" | "retired" | "unknown")) : this {
        Object.assign(this.resource, { status: value });
        return this;
    }

    getExposureBackground () : Reference<"EvidenceVariable"> | undefined {
        return this.resource.exposureBackground as Reference<"EvidenceVariable"> | undefined;
    }

    setExposureBackground (value: Reference<"EvidenceVariable">) : this {
        Object.assign(this.resource, { exposureBackground: value });
        return this;
    }

    getExposureVariant () : Reference<"EvidenceVariable">[] | undefined {
        return this.resource.exposureVariant as Reference<"EvidenceVariable">[] | undefined;
    }

    setExposureVariant (value: Reference<"EvidenceVariable">[]) : this {
        Object.assign(this.resource, { exposureVariant: value });
        return this;
    }

    getOutcome () : Reference<"EvidenceVariable">[] | undefined {
        return this.resource.outcome as Reference<"EvidenceVariable">[] | undefined;
    }

    setOutcome (value: Reference<"EvidenceVariable">[]) : this {
        Object.assign(this.resource, { outcome: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "Evidence Synthesis Profile"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "status"),
                ...validateEnum(res, profileName, "status", ["draft","active","retired","unknown"]),
                ...validateRequired(res, profileName, "exposureBackground"),
                ...validateReference(res, profileName, "exposureBackground", ["EvidenceVariable"]),
                ...validateRequired(res, profileName, "exposureVariant"),
                ...validateReference(res, profileName, "exposureVariant", ["EvidenceVariable"]),
                ...validateRequired(res, profileName, "outcome"),
                ...validateReference(res, profileName, "outcome", ["EvidenceVariable"]),
            ],
            warnings: [],
        }
    }

}

