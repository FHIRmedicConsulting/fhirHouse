// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type sctdescidProfileRaw = {
    valueId: string;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/coding-sctdescid (pkg: hl7.fhir.r4.core#4.0.1)
export class sctdescidProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/coding-sctdescid";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : sctdescidProfile {
        const profile = new sctdescidProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : sctdescidProfile {
        return new sctdescidProfile(resource);
    }

    static createResource (args: sctdescidProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/coding-sctdescid",
            valueId: args.valueId,
        })
        return resource;
    }

    static create (args: sctdescidProfileRaw) : sctdescidProfile {
        return sctdescidProfile.apply(sctdescidProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValueId () : string | undefined {
        return this.resource.valueId as string | undefined;
    }

    setValueId (value: string) : this {
        Object.assign(this.resource, { valueId: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "sctdescid"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", sctdescidProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valueId"]),
            ],
            warnings: [],
        }
    }

}

