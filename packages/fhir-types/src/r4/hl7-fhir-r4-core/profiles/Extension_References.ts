// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { CodeableConcept } from "../../hl7-fhir-r4-core/CodeableConcept";
import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    isRawExtensionInput,
    isExtension,
    getExtensionValue,
    pushExtension,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type ReferencesProfileRaw = {
    extension?: Extension[];
}

export type ReferencesProfileFlat = {
    description?: string;
    reference?: string[];
    type?: CodeableConcept;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/DiagnosticReport-geneticsReferences (pkg: hl7.fhir.r4.core#4.0.1)
export class ReferencesProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/DiagnosticReport-geneticsReferences";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : ReferencesProfile {
        const profile = new ReferencesProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : ReferencesProfile {
        return new ReferencesProfile(resource);
    }

    private static resolveInput (args: ReferencesProfileRaw | ReferencesProfileFlat) : Extension[] {
        if (isRawExtensionInput<ReferencesProfileRaw>(args)) {
            return args.extension ?? [];
        } else {
            const result: Extension[] = [];
            if (args.description !== undefined) {
                result.push({ url: "description", valueString: args.description } as Extension);
            }
            if (args.reference) {
                for (const item of args.reference) {
                    result.push({ url: "reference", valueUri: item } as Extension);
                }
            }
            if (args.type !== undefined) {
                result.push({ url: "type", valueCodeableConcept: args.type } as Extension);
            }
            return result;
        }
    }

    static createResource (args?: ReferencesProfileRaw | ReferencesProfileFlat) : Extension {
        const resolvedExtensions = ReferencesProfile.resolveInput(args ?? {});

        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/DiagnosticReport-geneticsReferences",
            extension: resolvedExtensions,
        })
        return resource;
    }

    static create (args?: ReferencesProfileRaw | ReferencesProfileFlat) : ReferencesProfile {
        return ReferencesProfile.apply(ReferencesProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    public setDescription (value: string): this {
        pushExtension(this.resource, { url: "description", valueString: value } as Extension)
        return this
    }

    public getDescription(mode: 'flat'): string | undefined;
    public getDescription(mode: 'raw'): Extension | undefined;
    public getDescription(): string | undefined;
    public getDescription (mode: 'flat' | 'raw' = 'flat'): string | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "description")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<string>(ext, "valueString")
    }

    public setReference (value: string): this {
        pushExtension(this.resource, { url: "reference", valueUri: value } as Extension)
        return this
    }

    public getReference(mode: 'flat'): string | undefined;
    public getReference(mode: 'raw'): Extension | undefined;
    public getReference(): string | undefined;
    public getReference (mode: 'flat' | 'raw' = 'flat'): string | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "reference")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<string>(ext, "valueUri")
    }

    public setType (value: CodeableConcept): this {
        pushExtension(this.resource, { url: "type", valueCodeableConcept: value } as Extension)
        return this
    }

    public getType(mode: 'flat'): CodeableConcept | undefined;
    public getType(mode: 'raw'): Extension | undefined;
    public getType(): CodeableConcept | undefined;
    public getType (mode: 'flat' | 'raw' = 'flat'): CodeableConcept | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "type")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<CodeableConcept>(ext, "valueCodeableConcept")
    }

    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "References"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", ReferencesProfile.canonicalUrl),
            ],
            warnings: [],
        }
    }

}

