// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Attachment } from "../../hl7-fhir-r4-core/Attachment";
import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type boundary_geojsonProfileRaw = {
    valueAttachment: Attachment;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/location-boundary-geojson (pkg: hl7.fhir.r4.core#4.0.1)
export class boundary_geojsonProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/location-boundary-geojson";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : boundary_geojsonProfile {
        const profile = new boundary_geojsonProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : boundary_geojsonProfile {
        return new boundary_geojsonProfile(resource);
    }

    static createResource (args: boundary_geojsonProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/location-boundary-geojson",
            valueAttachment: args.valueAttachment,
        })
        return resource;
    }

    static create (args: boundary_geojsonProfileRaw) : boundary_geojsonProfile {
        return boundary_geojsonProfile.apply(boundary_geojsonProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValueAttachment () : Attachment | undefined {
        return this.resource.valueAttachment as Attachment | undefined;
    }

    setValueAttachment (value: Attachment) : this {
        Object.assign(this.resource, { valueAttachment: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "boundary-geojson"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", boundary_geojsonProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valueAttachment"]),
            ],
            warnings: [],
        }
    }

}

