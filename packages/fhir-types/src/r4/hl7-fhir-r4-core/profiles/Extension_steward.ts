// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { ContactDetail } from "../../hl7-fhir-r4-core/ContactDetail";
import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type stewardProfileRaw = {
    valueContactDetail: ContactDetail;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/valueset-steward (pkg: hl7.fhir.r4.core#4.0.1)
export class stewardProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/valueset-steward";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : stewardProfile {
        const profile = new stewardProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : stewardProfile {
        return new stewardProfile(resource);
    }

    static createResource (args: stewardProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/valueset-steward",
            valueContactDetail: args.valueContactDetail,
        })
        return resource;
    }

    static create (args: stewardProfileRaw) : stewardProfile {
        return stewardProfile.apply(stewardProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValueContactDetail () : ContactDetail | undefined {
        return this.resource.valueContactDetail as ContactDetail | undefined;
    }

    setValueContactDetail (value: ContactDetail) : this {
        Object.assign(this.resource, { valueContactDetail: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "steward"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", stewardProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valueContactDetail"]),
            ],
            warnings: [],
        }
    }

}

