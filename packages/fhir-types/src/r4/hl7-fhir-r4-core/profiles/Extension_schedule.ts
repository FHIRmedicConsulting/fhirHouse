// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";
import type { Timing } from "../../hl7-fhir-r4-core/Timing";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type scheduleProfileRaw = {
    valueTiming: Timing;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/procedure-schedule (pkg: hl7.fhir.r4.core#4.0.1)
export class scheduleProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/procedure-schedule";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : scheduleProfile {
        const profile = new scheduleProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : scheduleProfile {
        return new scheduleProfile(resource);
    }

    static createResource (args: scheduleProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/procedure-schedule",
            valueTiming: args.valueTiming,
        })
        return resource;
    }

    static create (args: scheduleProfileRaw) : scheduleProfile {
        return scheduleProfile.apply(scheduleProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValueTiming () : Timing | undefined {
        return this.resource.valueTiming as Timing | undefined;
    }

    setValueTiming (value: Timing) : this {
        Object.assign(this.resource, { valueTiming: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "schedule"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", scheduleProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valueTiming"]),
            ],
            warnings: [],
        }
    }

}

