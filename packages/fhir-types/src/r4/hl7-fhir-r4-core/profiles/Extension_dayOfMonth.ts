// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type dayOfMonthProfileRaw = {
    valuePositiveInt: number;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/timing-dayOfMonth (pkg: hl7.fhir.r4.core#4.0.1)
export class dayOfMonthProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/timing-dayOfMonth";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : dayOfMonthProfile {
        const profile = new dayOfMonthProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : dayOfMonthProfile {
        return new dayOfMonthProfile(resource);
    }

    static createResource (args: dayOfMonthProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/timing-dayOfMonth",
            valuePositiveInt: args.valuePositiveInt,
        })
        return resource;
    }

    static create (args: dayOfMonthProfileRaw) : dayOfMonthProfile {
        return dayOfMonthProfile.apply(dayOfMonthProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValuePositiveInt () : number | undefined {
        return this.resource.valuePositiveInt as number | undefined;
    }

    setValuePositiveInt (value: number) : this {
        Object.assign(this.resource, { valuePositiveInt: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "dayOfMonth"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", dayOfMonthProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valuePositiveInt"]),
            ],
            warnings: [],
        }
    }

}

