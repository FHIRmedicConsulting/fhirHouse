// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Duration } from "../../hl7-fhir-r4-core/Duration";
import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type exposureDurationProfileRaw = {
    valueDuration: Duration;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/openEHR-exposureDuration (pkg: hl7.fhir.r4.core#4.0.1)
export class exposureDurationProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/openEHR-exposureDuration";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : exposureDurationProfile {
        const profile = new exposureDurationProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : exposureDurationProfile {
        return new exposureDurationProfile(resource);
    }

    static createResource (args: exposureDurationProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/openEHR-exposureDuration",
            valueDuration: args.valueDuration,
        })
        return resource;
    }

    static create (args: exposureDurationProfileRaw) : exposureDurationProfile {
        return exposureDurationProfile.apply(exposureDurationProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValueDuration () : Duration | undefined {
        return this.resource.valueDuration as Duration | undefined;
    }

    setValueDuration (value: Duration) : this {
        Object.assign(this.resource, { valueDuration: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "exposureDuration"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", exposureDurationProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valueDuration"]),
            ],
            warnings: [],
        }
    }

}

