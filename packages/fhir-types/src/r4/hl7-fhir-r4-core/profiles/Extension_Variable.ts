// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Expression } from "../../hl7-fhir-r4-core/Expression";
import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type VariableProfileRaw = {
    valueExpression: Expression;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/variable (pkg: hl7.fhir.r4.core#4.0.1)
export class VariableProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/variable";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : VariableProfile {
        const profile = new VariableProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : VariableProfile {
        return new VariableProfile(resource);
    }

    static createResource (args: VariableProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/variable",
            valueExpression: args.valueExpression,
        })
        return resource;
    }

    static create (args: VariableProfileRaw) : VariableProfile {
        return VariableProfile.apply(VariableProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValueExpression () : Expression | undefined {
        return this.resource.valueExpression as Expression | undefined;
    }

    setValueExpression (value: Expression) : this {
        Object.assign(this.resource, { valueExpression: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "Variable"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", VariableProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valueExpression"]),
            ],
            warnings: [],
        }
    }

}

