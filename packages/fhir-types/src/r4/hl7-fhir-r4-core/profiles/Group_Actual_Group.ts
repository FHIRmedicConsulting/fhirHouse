// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Group } from "../../hl7-fhir-r4-core/Group";

import {
    buildResource,
    ensureProfile,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type Actual_GroupProfileRaw = {
    type: ("person" | "animal" | "practitioner" | "device" | "medication" | "substance");
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/actualgroup (pkg: hl7.fhir.r4.core#4.0.1)
export class Actual_GroupProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/actualgroup";

    private resource: Group;

    constructor (resource: Group) {
        this.resource = resource;
    }

    static from (resource: Group) : Actual_GroupProfile {
        if (!resource.meta?.profile?.includes(Actual_GroupProfile.canonicalUrl)) {
            throw new Error(`Actual_GroupProfile: meta.profile must include ${Actual_GroupProfile.canonicalUrl}`)
        }
        const profile = new Actual_GroupProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Group) : Actual_GroupProfile {
        ensureProfile(resource, Actual_GroupProfile.canonicalUrl);
        return new Actual_GroupProfile(resource);
    }

    static createResource (args: Actual_GroupProfileRaw) : Group {
        const resource = buildResource<Group>( {
            resourceType: "Group",
            actual: true,
            type: args.type,
            meta: { profile: [Actual_GroupProfile.canonicalUrl] },
        })
        return resource;
    }

    static create (args: Actual_GroupProfileRaw) : Actual_GroupProfile {
        return Actual_GroupProfile.apply(Actual_GroupProfile.createResource(args));
    }

    toResource () : Group {
        return this.resource;
    }

    // Field accessors
    getType () : ("person" | "animal" | "practitioner" | "device" | "medication" | "substance") | undefined {
        return this.resource.type as ("person" | "animal" | "practitioner" | "device" | "medication" | "substance") | undefined;
    }

    setType (value: ("person" | "animal" | "practitioner" | "device" | "medication" | "substance")) : this {
        Object.assign(this.resource, { type: value });
        return this;
    }

    getActual () : boolean | undefined {
        return this.resource.actual as boolean | undefined;
    }

    setActual (value: boolean) : this {
        Object.assign(this.resource, { actual: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "Actual Group"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "actual"),
                ...validateFixedValue(res, profileName, "actual", true),
            ],
            warnings: [],
        }
    }

}

