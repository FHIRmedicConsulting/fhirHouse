// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    isRawExtensionInput,
    isExtension,
    getExtensionValue,
    pushExtension,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type TranslationProfileRaw = {
    extension: Extension[];
}

export type TranslationProfileFlat = {
    lang: string;
    content: string;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/translation (pkg: hl7.fhir.r4.core#4.0.1)
export class TranslationProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/translation";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : TranslationProfile {
        const profile = new TranslationProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : TranslationProfile {
        return new TranslationProfile(resource);
    }

    private static resolveInput (args: TranslationProfileRaw | TranslationProfileFlat) : Extension[] {
        if (isRawExtensionInput<TranslationProfileRaw>(args)) {
            return args.extension ?? [];
        } else {
            const result: Extension[] = [];
            if (args.lang !== undefined) {
                result.push({ url: "lang", valueCode: args.lang } as Extension);
            }
            if (args.content !== undefined) {
                result.push({ url: "content", valueString: args.content } as Extension);
            }
            return result;
        }
    }

    static createResource (args: TranslationProfileRaw | TranslationProfileFlat) : Extension {
        const resolvedExtensions = TranslationProfile.resolveInput(args ?? {});

        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/translation",
            extension: resolvedExtensions,
        })
        return resource;
    }

    static create (args: TranslationProfileRaw | TranslationProfileFlat) : TranslationProfile {
        return TranslationProfile.apply(TranslationProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getExtension () : Extension[] | undefined {
        return this.resource.extension as Extension[] | undefined;
    }

    setExtension (value: Extension[]) : this {
        Object.assign(this.resource, { extension: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    public setLang (value: string): this {
        pushExtension(this.resource, { url: "lang", valueCode: value } as Extension)
        return this
    }

    public getLang(mode: 'flat'): string | undefined;
    public getLang(mode: 'raw'): Extension | undefined;
    public getLang(): string | undefined;
    public getLang (mode: 'flat' | 'raw' = 'flat'): string | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "lang")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<string>(ext, "valueCode")
    }

    public setContent (value: string): this {
        pushExtension(this.resource, { url: "content", valueString: value } as Extension)
        return this
    }

    public getContent(mode: 'flat'): string | undefined;
    public getContent(mode: 'raw'): Extension | undefined;
    public getContent(): string | undefined;
    public getContent (mode: 'flat' | 'raw' = 'flat'): string | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "content")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        return getExtensionValue<string>(ext, "valueString")
    }

    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "Translation"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "extension"),
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", TranslationProfile.canonicalUrl),
            ],
            warnings: [],
        }
    }

}

