// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type supplementProfileRaw = {
    valueCanonical: string;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/valueset-supplement (pkg: hl7.fhir.r4.core#4.0.1)
export class supplementProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/valueset-supplement";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : supplementProfile {
        const profile = new supplementProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : supplementProfile {
        return new supplementProfile(resource);
    }

    static createResource (args: supplementProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/valueset-supplement",
            valueCanonical: args.valueCanonical,
        })
        return resource;
    }

    static create (args: supplementProfileRaw) : supplementProfile {
        return supplementProfile.apply(supplementProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValueCanonical () : string | undefined {
        return this.resource.valueCanonical as string | undefined;
    }

    setValueCanonical (value: string) : this {
        Object.assign(this.resource, { valueCanonical: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "supplement"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", supplementProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valueCanonical"]),
            ],
            warnings: [],
        }
    }

}

