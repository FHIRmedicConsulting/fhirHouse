// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Distance } from "../../hl7-fhir-r4-core/Distance";
import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type location_distanceProfileRaw = {
    valueDistance: Distance;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/location-distance (pkg: hl7.fhir.r4.core#4.0.1)
export class location_distanceProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/location-distance";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : location_distanceProfile {
        const profile = new location_distanceProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : location_distanceProfile {
        return new location_distanceProfile(resource);
    }

    static createResource (args: location_distanceProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/location-distance",
            valueDistance: args.valueDistance,
        })
        return resource;
    }

    static create (args: location_distanceProfileRaw) : location_distanceProfile {
        return location_distanceProfile.apply(location_distanceProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValueDistance () : Distance | undefined {
        return this.resource.valueDistance as Distance | undefined;
    }

    setValueDistance (value: Distance) : this {
        Object.assign(this.resource, { valueDistance: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "location-distance"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", location_distanceProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valueDistance"]),
            ],
            warnings: [],
        }
    }

}

