// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type TEL_addressProfileRaw = {
    valueUrl: string;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/iso21090-TEL-address (pkg: hl7.fhir.r4.core#4.0.1)
export class TEL_addressProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/iso21090-TEL-address";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : TEL_addressProfile {
        const profile = new TEL_addressProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : TEL_addressProfile {
        return new TEL_addressProfile(resource);
    }

    static createResource (args: TEL_addressProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/iso21090-TEL-address",
            valueUrl: args.valueUrl,
        })
        return resource;
    }

    static create (args: TEL_addressProfileRaw) : TEL_addressProfile {
        return TEL_addressProfile.apply(TEL_addressProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValueUrl () : string | undefined {
        return this.resource.valueUrl as string | undefined;
    }

    setValueUrl (value: string) : this {
        Object.assign(this.resource, { valueUrl: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "TEL-address"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", TEL_addressProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valueUrl"]),
            ],
            warnings: [],
        }
    }

}

