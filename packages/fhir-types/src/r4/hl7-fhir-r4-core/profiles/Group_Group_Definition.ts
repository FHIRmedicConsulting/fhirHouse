// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Group } from "../../hl7-fhir-r4-core/Group";

import {
    buildResource,
    ensureProfile,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type Group_DefinitionProfileRaw = {
    type: ("person" | "animal" | "practitioner" | "device" | "medication" | "substance");
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/groupdefinition (pkg: hl7.fhir.r4.core#4.0.1)
export class Group_DefinitionProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/groupdefinition";

    private resource: Group;

    constructor (resource: Group) {
        this.resource = resource;
    }

    static from (resource: Group) : Group_DefinitionProfile {
        if (!resource.meta?.profile?.includes(Group_DefinitionProfile.canonicalUrl)) {
            throw new Error(`Group_DefinitionProfile: meta.profile must include ${Group_DefinitionProfile.canonicalUrl}`)
        }
        const profile = new Group_DefinitionProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Group) : Group_DefinitionProfile {
        ensureProfile(resource, Group_DefinitionProfile.canonicalUrl);
        return new Group_DefinitionProfile(resource);
    }

    static createResource (args: Group_DefinitionProfileRaw) : Group {
        const resource = buildResource<Group>( {
            resourceType: "Group",
            actual: false,
            type: args.type,
            meta: { profile: [Group_DefinitionProfile.canonicalUrl] },
        })
        return resource;
    }

    static create (args: Group_DefinitionProfileRaw) : Group_DefinitionProfile {
        return Group_DefinitionProfile.apply(Group_DefinitionProfile.createResource(args));
    }

    toResource () : Group {
        return this.resource;
    }

    // Field accessors
    getType () : ("person" | "animal" | "practitioner" | "device" | "medication" | "substance") | undefined {
        return this.resource.type as ("person" | "animal" | "practitioner" | "device" | "medication" | "substance") | undefined;
    }

    setType (value: ("person" | "animal" | "practitioner" | "device" | "medication" | "substance")) : this {
        Object.assign(this.resource, { type: value });
        return this;
    }

    getActual () : boolean | undefined {
        return this.resource.actual as boolean | undefined;
    }

    setActual (value: boolean) : this {
        Object.assign(this.resource, { actual: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "Group Definition"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "actual"),
                ...validateFixedValue(res, profileName, "actual", false),
            ],
            warnings: [],
        }
    }

}

