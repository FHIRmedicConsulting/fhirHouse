// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type hierarchyProfileRaw = {
    valueBoolean: boolean;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/structuredefinition-hierarchy (pkg: hl7.fhir.r4.core#4.0.1)
export class hierarchyProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/structuredefinition-hierarchy";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : hierarchyProfile {
        const profile = new hierarchyProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : hierarchyProfile {
        return new hierarchyProfile(resource);
    }

    static createResource (args: hierarchyProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/structuredefinition-hierarchy",
            valueBoolean: args.valueBoolean,
        })
        return resource;
    }

    static create (args: hierarchyProfileRaw) : hierarchyProfile {
        return hierarchyProfile.apply(hierarchyProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValueBoolean () : boolean | undefined {
        return this.resource.valueBoolean as boolean | undefined;
    }

    setValueBoolean (value: boolean) : this {
        Object.assign(this.resource, { valueBoolean: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "hierarchy"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", hierarchyProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valueBoolean"]),
            ],
            warnings: [],
        }
    }

}

