// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { CodeableConcept } from "../../hl7-fhir-r4-core/CodeableConcept";
import type { Extension } from "../../hl7-fhir-r4-core/Extension";
import type { Reference } from "../../hl7-fhir-r4-core/Reference";
import type { ServiceRequest } from "../../hl7-fhir-r4-core/ServiceRequest";

import { ItemProfile, type ItemProfileFlat } from "./Extension_Item";

import {
    buildResource,
    ensureProfile,
    extractComplexExtension,
    isExtension,
    getExtensionValue,
    pushExtension,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type ServiceRequest_GeneticsProfileRaw = {
    status: ("draft" | "active" | "on-hold" | "revoked" | "completed" | "entered-in-error" | "unknown");
    intent: ("proposal" | "plan" | "directive" | "order" | "original-order" | "reflex-order" | "filler-order" | "instance-order" | "option");
    subject: Reference<"Device" | "Group" | "Location" | "Patient">;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/servicerequest-genetics (pkg: hl7.fhir.r4.core#4.0.1)
export class ServiceRequest_GeneticsProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/servicerequest-genetics";

    private resource: ServiceRequest;

    constructor (resource: ServiceRequest) {
        this.resource = resource;
    }

    static from (resource: ServiceRequest) : ServiceRequest_GeneticsProfile {
        if (!resource.meta?.profile?.includes(ServiceRequest_GeneticsProfile.canonicalUrl)) {
            throw new Error(`ServiceRequest_GeneticsProfile: meta.profile must include ${ServiceRequest_GeneticsProfile.canonicalUrl}`)
        }
        const profile = new ServiceRequest_GeneticsProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: ServiceRequest) : ServiceRequest_GeneticsProfile {
        ensureProfile(resource, ServiceRequest_GeneticsProfile.canonicalUrl);
        return new ServiceRequest_GeneticsProfile(resource);
    }

    static createResource (args: ServiceRequest_GeneticsProfileRaw) : ServiceRequest {
        const resource = buildResource<ServiceRequest>( {
            resourceType: "ServiceRequest",
            status: args.status,
            intent: args.intent,
            subject: args.subject,
            meta: { profile: [ServiceRequest_GeneticsProfile.canonicalUrl] },
        })
        return resource;
    }

    static create (args: ServiceRequest_GeneticsProfileRaw) : ServiceRequest_GeneticsProfile {
        return ServiceRequest_GeneticsProfile.apply(ServiceRequest_GeneticsProfile.createResource(args));
    }

    toResource () : ServiceRequest {
        return this.resource;
    }

    // Field accessors
    getStatus () : ("draft" | "active" | "on-hold" | "revoked" | "completed" | "entered-in-error" | "unknown") | undefined {
        return this.resource.status as ("draft" | "active" | "on-hold" | "revoked" | "completed" | "entered-in-error" | "unknown") | undefined;
    }

    setStatus (value: ("draft" | "active" | "on-hold" | "revoked" | "completed" | "entered-in-error" | "unknown")) : this {
        Object.assign(this.resource, { status: value });
        return this;
    }

    getIntent () : ("proposal" | "plan" | "directive" | "order" | "original-order" | "reflex-order" | "filler-order" | "instance-order" | "option") | undefined {
        return this.resource.intent as ("proposal" | "plan" | "directive" | "order" | "original-order" | "reflex-order" | "filler-order" | "instance-order" | "option") | undefined;
    }

    setIntent (value: ("proposal" | "plan" | "directive" | "order" | "original-order" | "reflex-order" | "filler-order" | "instance-order" | "option")) : this {
        Object.assign(this.resource, { intent: value });
        return this;
    }

    getSubject () : Reference<"Device" | "Group" | "Location" | "Patient"> | undefined {
        return this.resource.subject as Reference<"Device" | "Group" | "Location" | "Patient"> | undefined;
    }

    setSubject (value: Reference<"Device" | "Group" | "Location" | "Patient">) : this {
        Object.assign(this.resource, { subject: value });
        return this;
    }

    // Extensions
    public setItem (input: ItemProfileFlat | ItemProfile | Extension): this {
        if (input instanceof ItemProfile) {
            pushExtension(this.resource, input.toResource())
        } else if (isExtension<Extension>(input)) {
            if (input.url !== "http://hl7.org/fhir/StructureDefinition/servicerequest-geneticsItem") throw new Error(`Expected extension url 'http://hl7.org/fhir/StructureDefinition/servicerequest-geneticsItem', got '${input.url}'`)
            pushExtension(this.resource, input)
        } else {
            pushExtension(this.resource, ItemProfile.createResource(input))
        }
        return this
    }

    public getItem(mode: 'flat'): ItemProfileFlat | undefined;
    public getItem(mode: 'profile'): ItemProfile | undefined;
    public getItem(mode: 'raw'): Extension | undefined;
    public getItem(): ItemProfileFlat | undefined;
    public getItem (mode: 'flat' | 'profile' | 'raw' = 'flat'): ItemProfileFlat | ItemProfile | Extension | undefined {
        const ext = this.resource.extension?.find(e => e.url === "http://hl7.org/fhir/StructureDefinition/servicerequest-geneticsItem")
        if (!ext) return undefined
        if (mode === 'raw') return ext
        if (mode === 'profile') return ItemProfile.apply(ext)
        const config = [{ name: "code", valueField: "valueCodeableConcept", isArray: false }, { name: "geneticsObservation", valueField: "valueReference", isArray: false }, { name: "specimen", valueField: "valueReference", isArray: false }, { name: "status", valueField: "valueCode", isArray: false }]
        return extractComplexExtension<ItemProfileFlat>(ext, config)
    }

    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "ServiceRequest-Genetics"
        const res = this.resource
        return {
            errors: [],
            warnings: [],
        }
    }

}

