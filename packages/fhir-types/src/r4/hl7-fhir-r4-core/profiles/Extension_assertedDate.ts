// WARNING: This file is autogenerated by @atomic-ehr/codegen.
// GitHub: https://github.com/atomic-ehr/codegen
// Any manual changes made to this file may be overwritten.

import type { Extension } from "../../hl7-fhir-r4-core/Extension";

import {
    buildResource,
    validateRequired,
    validateExcluded,
    validateFixedValue,
    validateSliceCardinality,
    validateEnum,
    validateReference,
    validateChoiceRequired,
    validateMustSupport,
} from "../../profile-helpers";

export type assertedDateProfileRaw = {
    valueDateTime: string;
}

// CanonicalURL: http://hl7.org/fhir/StructureDefinition/condition-assertedDate (pkg: hl7.fhir.r4.core#4.0.1)
export class assertedDateProfile {
    static readonly canonicalUrl = "http://hl7.org/fhir/StructureDefinition/condition-assertedDate";

    private resource: Extension;

    constructor (resource: Extension) {
        this.resource = resource;
    }

    static from (resource: Extension) : assertedDateProfile {
        const profile = new assertedDateProfile(resource);
        const { errors } = profile.validate();
        if (errors.length > 0) throw new Error(errors.join("; "))
        return profile;
    }

    static apply (resource: Extension) : assertedDateProfile {
        return new assertedDateProfile(resource);
    }

    static createResource (args: assertedDateProfileRaw) : Extension {
        const resource = buildResource<Extension>( {
            url: "http://hl7.org/fhir/StructureDefinition/condition-assertedDate",
            valueDateTime: args.valueDateTime,
        })
        return resource;
    }

    static create (args: assertedDateProfileRaw) : assertedDateProfile {
        return assertedDateProfile.apply(assertedDateProfile.createResource(args));
    }

    toResource () : Extension {
        return this.resource;
    }

    // Field accessors
    getValueDateTime () : string | undefined {
        return this.resource.valueDateTime as string | undefined;
    }

    setValueDateTime (value: string) : this {
        Object.assign(this.resource, { valueDateTime: value });
        return this;
    }

    getUrl () : string | undefined {
        return this.resource.url as string | undefined;
    }

    setUrl (value: string) : this {
        Object.assign(this.resource, { url: value });
        return this;
    }

    // Extensions
    // Slices
    // Validation
    validate(): { errors: string[]; warnings: string[] } {
        const profileName = "assertedDate"
        const res = this.resource
        return {
            errors: [
                ...validateRequired(res, profileName, "url"),
                ...validateFixedValue(res, profileName, "url", assertedDateProfile.canonicalUrl),
                ...validateChoiceRequired(res, profileName, ["valueDateTime"]),
            ],
            warnings: [],
        }
    }

}

