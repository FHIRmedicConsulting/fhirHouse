/**
 * fhirEngine FHIR types — generated entry point.
 *
 * Re-exports the R4 catalog from the generated tree. Per the codegen strategy
 * note ([docs/research/2026-06-22-fhir-codegen-strategy.md](../../../docs/research/2026-06-22-fhir-codegen-strategy.md))
 * everything in `src/r4/` is autogenerated by `@atomic-ehr/codegen` from spec.
 * Do not hand-edit.
 *
 * IG profile narrowings (US Core, CARIN BB, etc.) land in `src/r4/profiles/`
 * as they're added to `generate.ts`. v1 floor: R4 core (committed); IGs add
 * incrementally without breaking this barrel.
 */

// Re-export every R4 core resource type, datatype, BackboneElement, and the
// `is<ResourceType>` runtime type guards.
export * from "./r4/hl7-fhir-r4-core/index.js";

// Re-export the runtime profile-helpers (slice + extension + choice-type
// utilities) for resources that consume profile classes.
export * from "./r4/profile-helpers.js";

// US Core 6.1.0 — hand-written profile narrowings until codegen can be
// re-run end-to-end (currently EMFILE-blocked in sandboxes). v1 ships
// us-core-patient; v1.x lands the remainder via codegen.
export * from "./r4/hl7-fhir-us-core/index.js";
