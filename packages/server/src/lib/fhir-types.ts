/**
 * Deprecated compatibility stub — content removed in session 022 (A2 migration).
 *
 * All FHIR resource types + datatypes + type guards now live in
 * `@fhirengine/fhir-types` (generated by `@atomic-ehr/codegen`). fhirEngine-side
 * utilities (`patientIdFromReference`, `SearchsetBundle<T>`) moved to
 * `./fhirengine-fhir-utils.ts`.
 *
 * This file remains empty so the sandbox's append-only mount doesn't error
 * on the unlink that we couldn't perform. Safe to delete from the host shell:
 *     rm packages/server/src/lib/fhir-types.ts
 *
 * Per [`docs/research/2026-06-23-server-migration-plan.md`](../../../../docs/research/2026-06-23-server-migration-plan.md) §5 step 5.
 */

export {};
